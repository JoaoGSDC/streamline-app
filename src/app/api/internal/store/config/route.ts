import {
  getStoreConfigController,
  patchStoreConfigController,
} from "@api/internal/store/store.controller";

export const GET = getStoreConfigController;
export const PATCH = patchStoreConfigController;
